services:
  web:
    build:
      context: ./backend
    container_name: crabgator-web
    restart: unless-stopped
    env_file:
      - ./backend/.env
    ports:
      - "127.0.0.1:8000:8000"
    command: gunicorn config.wsgi:application --bind 0.0.0.0:8000